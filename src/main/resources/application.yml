spring:
  application:
    name: MySpringBoot2Dbase
  datasource:
    url: jdbc:h2:/home/barsegyanvl/projects/java/MySpringBoot2Dbase/testdb/students
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  h2:
    console:
      enabled: true

server:
  port: 8082
